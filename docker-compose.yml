services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: production
    ports:
      - "9000:9000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
